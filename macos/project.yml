name: ctail
# Generates ctail.xcodeproj for App Store / TestFlight distribution.
# Run:  cd macos && xcodegen generate && open ctail.xcodeproj
# The .xcodeproj and generated Info.plist are git-ignored; this file is the source
# of truth. The SwiftPM package (Package.swift) stays for CLI/dev/self-test builds.
options:
  bundleIdPrefix: no.bogentech
  deploymentTarget:
    macOS: "13.0"
  createIntermediateGroups: true
  groupSortPosition: top

settings:
  base:
    MARKETING_VERSION: "0.9.9"
    CURRENT_PROJECT_VERSION: "255"
    SWIFT_VERSION: "5.0"
    CODE_SIGN_STYLE: Automatic
    # Apple Developer team (matches the "Apple Distribution: André Biseth" identity).
    # The CI workflow overrides this from the APPLE_TEAM_ID secret.
    DEVELOPMENT_TEAM: "5SX2347529"

targets:
  # Swift bindings for the Rust engine (generated by scripts/build-core.sh) as a
  # static library module, linking the universal static xcframework. Mirrors the
  # CtailCore / CtailCoreFFI targets in Package.swift.
  CtailCore:
    type: library.static
    platform: macOS
    deploymentTarget: "13.0"
    sources:
      - path: Sources/CtailCore
    dependencies:
      - framework: Frameworks/CtailCoreFFI.xcframework
        embed: false
    settings:
      base:
        PRODUCT_NAME: CtailCore
        SKIP_INSTALL: YES

  ctail:
    type: application
    platform: macOS
    deploymentTarget: "13.0"
    sources:
      - path: Sources/ctailmac
        excludes:
          - "Resources/appicon.png"     # the app icon comes from the asset catalog
      - path: Assets.xcassets
      - path: ctail.storekit            # local StoreKit testing (Scheme ▸ Run ▸ Options)
        buildPhase: none
    dependencies:
      - target: CtailCore
      # Static library link deps don't propagate: the app links the engine too.
      - framework: Frameworks/CtailCoreFFI.xcframework
        embed: false
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: no.bogentech.ctail
        PRODUCT_NAME: ctail
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        CODE_SIGN_ENTITLEMENTS: Resources/ctail.entitlements
        COMBINE_HIDPI_IMAGES: YES
    info:
      path: Resources/Info.generated.plist
      properties:
        CFBundleName: ctail
        CFBundleDisplayName: ctail
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        LSMinimumSystemVersion: "13.0"
        NSHighResolutionCapable: true
        LSApplicationCategoryType: "public.app-category.developer-tools"
        NSHumanReadableCopyright: "Copyright © 2024–2026 André Biseth"
        # Only Apple-provided TLS (URLSession); skips the export-compliance prompt on every TestFlight build.
        ITSAppUsesNonExemptEncryption: false
        CFBundleDocumentTypes:
          - CFBundleTypeName: "Log File"
            CFBundleTypeRole: Viewer
            LSHandlerRank: Alternate
            CFBundleTypeExtensions: [log, txt, csv]
